<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.microsoft.device.dualscreen.screenmanager.tests">

    <application
        android:name=".utils.SampleApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".utils.SampleActivity" />
        <activity
            android:name=".utils.SimpleFirstActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden" />
        <activity
            android:name=".utils.SimpleSecondActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden" />

        <activity android:name=".utils.TransparentFirstActivity" />

        <activity
            android:name=".utils.TransparentSecondActivity"
            android:theme="@style/AppTheme.Transparent" />
    </application>

</manifest>